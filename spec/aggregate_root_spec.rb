require 'spec_helper'

module Sandthorn
  module AggregateRoot
    describe "response to" do
      # let(:subject) { Sandthorn::AggregateRoot }
      # it "should responde to save" do
      #   expect(subject).to respond_to(:save)
      # end    
      # it "should responde to find" do
      #   expect(subject).to respond_to(:find)
      # end
      # it "should responde to all" do
      #   expect(subject).to respond_to(:all)
      # end
      
    end
  end
end